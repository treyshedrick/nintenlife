import React from 'react';
import {Layout, Text} from '@ui-kitten/components';

const SplashScreen = () => {
  return (
    <Layout style={{flex: 1}}>
      <Text>Test</Text>
    </Layout>
  );
};

export default SplashScreen;
