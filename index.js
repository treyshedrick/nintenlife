/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './src/app';
import {name as appName} from './src/app/helpers/app.json';

AppRegistry.registerComponent(appName, () => App);
